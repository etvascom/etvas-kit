import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { Table, Button } from '../src'

<Meta title="Guides/Buttons" component={Button} />

# Variants

The `Button` component has a couple of variants:

<Preview>
  <Story name="all buttons">
      <Button variant='primary'>
        Primary button
      </Button>
  </Story>
</Preview>
