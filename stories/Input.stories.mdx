import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { Table, Input, Typography, Icon } from '../src'

<Meta title="Guides/Input" component={Input} />

# Input

The `Input` component has a couple of options:


## Placeholder

<Preview>
  <Story name='Placeholder'>
    <Input name='test' placeholder='a placeholder' />
  </Story>
</Preview>


## Error

The input will display errors underneath. The error can be a string or a React node.

<Preview>
  <Story name='Error'>
    <>
      <Typography variant='titleSmall'>Simple error</Typography>
      <Input name='test' error='Needs value' />
      <Typography variant='titleSmall'>Component error</Typography>
      <Input name='test' error={<span><Icon name='alertCircle' /> Required</span>} />
    </>
  </Story>
</Preview>


## Label

<Preview>
  <Story name='Label'>
    <>
      <Input name='test' label='Name' />
      <Input name='test' label={<span><Icon name='alertCircle' /> Name</span>} />
    </>
  </Story>
</Preview>


