import { Meta, Preview, Story } from '@storybook/addon-docs/blocks'
import { Icon, Input, Table, Typography } from '../src'

<Meta title='Guides/Input' component={Input} />

# Input

The `Input` component has a couple of options:

## Placeholder

<Preview>
  <Story name='Placeholder'>
    <Input name='test1' id='test' placeholder='a placeholder' />
  </Story>
</Preview>

## Error

The input will display errors underneath. The error can be a string or a React node.

<Preview>
  <Story name='Error'>
    <>
      <Typography variant='titleSmall'>Simple error</Typography>
      <Input id='test2' name='test' error='Needs value' />
      <Typography variant='titleSmall'>Component error</Typography>
      <Input
        id='test3'
        name='test'
        error={
          <span>
            <Icon name='alertCircle' /> Required
          </span>
        }
      />
    </>
  </Story>
</Preview>

## Label

<Preview>
  <Story name='Label'>
    <>
      <Input id='test4' name='test' label='Name' />
      <Input
        id='test5'
        name='test1'
        label={
          <span>
            <Icon name='alertCircle' /> Name
          </span>
        }
      />
    </>
  </Story>
</Preview>
